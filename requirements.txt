piper-tts>=1.3
sounddevice>=0.4
numpy>=1.24
rich>=13
resvg-py>=0.5   # draws the SVG pictures (small, no other libraries needed)
vosk>=0.3.44  # 0.3.45 has no macOS or 32-bit wheel; pip picks the newest that fits
